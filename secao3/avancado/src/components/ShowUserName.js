

const showUserName = (props) =>{
    return(
        <div>
            <h2> Esse é o nome do usuario com sendo exibido com props: {props.name}</h2>
        </div>
    )
}

export default showUserName;